import 'package:flutter/material.dart';

class AboutUserStep extends StatelessWidget {
  const AboutUserStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
