import 'package:flutter/material.dart';

class MobilityStep extends StatelessWidget {
  const MobilityStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
