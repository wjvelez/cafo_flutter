import 'package:flutter/material.dart';

class ResideStep extends StatelessWidget {
  const ResideStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
