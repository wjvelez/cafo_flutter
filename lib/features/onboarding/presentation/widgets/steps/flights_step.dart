import 'package:flutter/material.dart';

class FlightsStep extends StatelessWidget {
  const FlightsStep({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
